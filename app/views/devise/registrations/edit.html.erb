<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Add your existing head content here -->
  <link rel="stylesheet" href="components/_edit_user.scss">
  <style>
    body {
      background-color: white;
    }

    #navhome {
      display: flex;
      justify-content: space-between;
      padding: 8px 16px;
      background-image: url("../assets/images/OffChangeX.png");
      background-repeat: no-repeat;
      background-position: center center;
      background-size: cover;
      color: white;
    }

    #navhome .logo {
      width: 100px; /* Adjust the width as needed */
      height: auto;
    }

    .edit-registration {
      margin-top: 20px;
    }

    .exchange-heading {
      margin-top: 12px;
      padding: 8px 24px;
      color: black;
      text-shadow: 0 0 0px;
      font-weight: 600;
      font-size: 28px;
      text-align: center;
    }

    .form-exchange input,
    .form-exchange select,
    .form-exchange textarea {
      border: 1px solid #ced4da;
      outline: none;
      box-shadow: none;
      border-radius: 8px;
      background-color: white;
      padding: 10px;
      font-size: 13px;
      color: #495057;
      margin-bottom: 10px;
    }

    .button-container {
      margin-top: 10px;
      text-align: center;
    }

  .form-inputs .email.form-label {
    font-size: 16px; /* Adjust the font size as needed */
  }
    .form-inputs .password.form-label {
    font-size: 16px; /* Adjust the font size as needed */
  }
    .form-inputs .password.required.form-label[for="user_current_password"] {
    font-size: 16px; /* Adjust the font size as needed */
  }

    .form-inputs {
      font-size: 16px; /* Adjusted font size */
      font-weight: 500;
      margin-bottom: 8px;
      color: black;
      border-radius: 8px;
      background-color: white;
    }

    #convert-button {
      background-color: #4CAF50;
      color: white;
      border: none;
      padding: 10px 20px;
      text-align: center;
      text-decoration: none;
      display: inline-block;
      font-size: 20px;
      margin-top: 24px;
      cursor: pointer;
      border-radius: 5px;
    }

    #currency-pairs {
      display: flex;
      flex-direction: column;
      align-items: center;
      color: white;
    }

    ul {
      list-style: none;
      padding: 0;
    }

    li {
      border-radius: 5px;
      padding: 10px;
      margin: 5px 0;
    }

    span {
      font-weight: bolder;
      margin-right: 10px;
      letter-spacing: 1px;
      color: white;
    }

 #convert-button.btn-update {
   padding: 10px 20px;
   background-image: linear-gradient(to bottom, #622aa3, #212492);
   color: goldenrod;
   text-decoration: none;
   border-radius: 4px;
   align-items: center;
   margin: 10px;
   font-weight: 500;
   font-size: 15px;
   border-style: none;
  /* Add other button styles here */
 }
  </style>
  <title>Your Webpage Title</title>
</head>
<body>
  <!-- Your HTML content here -->
    <div id="navhome">
       <img src="logo.png" alt="Logo" class="logo">
    </div>

  <h2 class="exchange-heading">Edit User</h2>
   <div class="edit-registration">
     <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, class: 'form-exchange' }) do |f| %>
      <%= f.error_notification %>

       <div class="form-inputs">
         <%= f.input :email, required: true, autofocus: true %>
          <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <p class="confirmation-message">Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
          <% end %>
         <%= f.input :password,
                    hint: "Leave it blank if you don't want to change it",
                    required: false %>
         <%= f.input :current_password,
                    hint: "We need your current password to confirm your changes",
                    required: true %>
      </div>

<div class="button-container">
   <%= f.button :submit, "Update", class: 'btn btn-update', id: 'convert-button' %>
</div>
<% end %>

<div class="cancel-section">
  Unhappy? <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, method: :delete, class: 'btn btn-cancel' %>
  <style>
    .cancel-section {
      margin-top: 20px;
      text-align: center;
      color: #777;
    }

    .cancel-message {
      font-size: 18px;
      margin-bottom: 10px;
    }

    .btn-cancel {
      background-color: #d9534f;
      color: white;
      border: none;
      padding: 8px 16px; /* Adjusted padding for a smaller size */
      text-align: center;
      text-decoration: none;
      display: inline-block;
      font-size: 16px; /* Adjusted font size for consistency */
      margin-top: 10px;
      cursor: pointer;
      border-radius: 5px;
    }

    .back-link {
      display: inline-block;
      margin-top: 10px;
      text-decoration: none;
      color: #333; /* Set color to your preference */
      font-size: 16px;
    }
  </style>
</div>

<%= link_to raw('<svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M9.16675 16.6667L3.33341 10L9.16675 3.33333L10.8334 5L7.50008 8.33333H16.6667V11.6667H7.50008L10.8334 15L9.16675 16.6667Z" fill="#333"/></svg>'), :back, class: 'back-link' %>

</div>

<!-- Add the rest of your HTML content here -->

<!-- Add your existing body scripts and content here -->
</body>
</html>
