<div class="wallets-form">
  <%= simple_form_for [@user, @wallet] do |f| %>
    <%= f.input :name %>
    <%= f.input :currency, collection: ["EUR", "USD", "GPB"] %>
    <%= f.input :main %>
    <%= f.button :submit %>
  <% end %>
</div>
